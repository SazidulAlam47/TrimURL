export interface IShortUrl {
    _id: string;
    shortId: string;
    originalUrl: string;
    user: string;
    clicks: number;
    shortUrl: string;
    createdAt: string;
    updatedAt: string;
}
